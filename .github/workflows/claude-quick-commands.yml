name: Claude Quick Commands

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  quick-command:
    if: |
      github.event.issue.pull_request &&
      (startsWith(github.event.comment.body, '/review') ||
       startsWith(github.event.comment.body, '/security') ||
       startsWith(github.event.comment.body, '/test') ||
       startsWith(github.event.comment.body, '/docs') ||
       startsWith(github.event.comment.body, '/refactor') ||
       startsWith(github.event.comment.body, '/debug') ||
       startsWith(github.event.comment.body, '/explain'))
    runs-on: ubuntu-latest
    steps:
      - name: Get PR info
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            core.setOutput('head_sha', pr.data.head.sha);
            core.setOutput('base_ref', pr.data.base.ref);

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ steps.pr.outputs.head_sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: React to comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'eyes'
            });

      - name: Run Quick Command
        id: run
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          COMMENT: ${{ github.event.comment.body }}
          BASE_REF: ${{ steps.pr.outputs.base_ref }}
        run: |
          set +x
          SANITIZED=$(echo "$COMMENT" | tr -cd '[:alnum:][:space:]-_./,')
          COMMAND=$(echo "$SANITIZED" | awk '{print $1}')
          EXTRA=$(echo "$SANITIZED" | sed 's|^/[^ ]* *||')
          case "$COMMAND" in
            "/review") PROMPT="Code review: $EXTRA"; TITLE="Code Review" ;;
            "/security") PROMPT="Security audit: $EXTRA"; TITLE="Security Audit" ;;
            "/test") PROMPT="Suggest tests: $EXTRA"; TITLE="Test Suggestions" ;;
            "/docs") PROMPT="Documentation review: $EXTRA"; TITLE="Documentation" ;;
            "/refactor") PROMPT="Refactoring suggestions: $EXTRA"; TITLE="Refactoring" ;;
            "/debug") PROMPT="Debug analysis: $EXTRA"; TITLE="Debug Analysis" ;;
            "/explain") PROMPT="Explain this code: $EXTRA"; TITLE="Code Explanation" ;;
            *) PROMPT="$SANITIZED"; TITLE="Claude Response" ;;
          esac
          echo "title=$TITLE" >> $GITHUB_OUTPUT
          git diff "origin/$BASE_REF"...HEAD > /tmp/diff.txt 2>/dev/null || true
          if ! claude --print "$PROMPT $(head -500 /tmp/diff.txt)" > /tmp/output.txt 2>&1; then
            echo "Command failed" >> /tmp/output.txt
          fi

      - name: Post result
        uses: actions/github-script@v7
        env:
          TITLE: ${{ steps.run.outputs.title }}
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('/tmp/output.txt', 'utf8');
            const title = process.env.TITLE;
            const maxLength = 60000;
            const truncated = output.length > maxLength ? output.substring(0, maxLength) + '\n... (truncated)' : output;
            const body = `## ${title}\n\n${truncated}\n\n---\n*Claude Code Agent*`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'rocket'
            });
